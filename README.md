# ForecastingcommodityETF

```r
# install.packages(c("shiny", "shinythemes", "forecast", "quantmod")) #Install these packages if needed

library(shiny)

library(shinythemes)

library(forecast)

library(quantmod)

runGitHub( "ForecastingcommodityETF", "deanfantazzini") 
```
